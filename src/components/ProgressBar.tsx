const ProgressBar = () => {
  return (
    <div className="w-full h-[13px] flex">
      <div className="w-full bg-gradient-to-r from-primary to-tertiary" />
    </div>
  );
};

export default ProgressBar;
